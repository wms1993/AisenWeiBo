<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--<item android:id="@+id/send" android:title="@string/publish_send" android:icon="@drawable/ic_action_send_now"-->
    <!--android:showAsAction="always"/>-->
    <group android:id="@+id/publish">
        <item
            android:id="@+id/group_all_visiable"
            android:orderInCategory="1"
            android:title="@string/group_all_visiable"
            app:showAsAction="never"/>
        <item
            android:id="@+id/publish_timing"
            android:orderInCategory="3"
            android:title="@string/publish_timing"
            app:showAsAction="never"/>
        <item
            android:id="@+id/cancel_timing"
            android:orderInCategory="4"
            android:title="@string/cancel_timing"
            app:showAsAction="never"/>
    </group>

</menu>
